name: Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'cloud/**'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/calpush-cloud

defaults:
  run:
    working-directory: cloud

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: cloud/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npx tsc

  build-and-push:
    runs-on: ubuntu-latest
    needs: ci
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: cloud
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # --- Deploy to Railway ---
  # Uncomment and set RAILWAY_TOKEN secret to enable
  #
  # deploy-railway:
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Install Railway CLI
  #       run: npm install -g @railway/cli
  #     - name: Deploy
  #       run: railway up --service calpush-cloud
  #       working-directory: cloud
  #       env:
  #         RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

  # --- Deploy to Fly.io ---
  # Uncomment and set FLY_API_TOKEN secret to enable
  #
  # deploy-fly:
  #   runs-on: ubuntu-latest
  #   needs: ci
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: superfly/flyctl-actions/setup-flyctl@master
  #     - name: Deploy
  #       run: flyctl deploy --remote-only
  #       working-directory: cloud
  #       env:
  #         FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  # --- Deploy to Cloud Run ---
  # Uncomment and configure Workload Identity Federation
  #
  # deploy-cloudrun:
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   permissions:
  #     contents: read
  #     id-token: write
  #   steps:
  #     - uses: google-github-actions/auth@v2
  #       with:
  #         workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
  #         service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
  #     - name: Deploy to Cloud Run
  #       uses: google-github-actions/deploy-cloudrun@v2
  #       with:
  #         service: calpush-cloud
  #         region: asia-northeast1
  #         image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
